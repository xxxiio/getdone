---
title: "Project TODO"
description: "Machine-readable source for skill/schemas/todo-frontmatter.schema.json."
tags: ["catalogue", "schema"]
---

<!-- Generated by python development/scripts/generate_docs.py. Do not edit directly. -->
# Project TODO

Authoritative source: `skill/schemas/todo-frontmatter.schema.json`

## Complete source

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Project TODO",
  "type": "object",
  "required": ["id", "status", "priority"],
  "properties": {
    "id": {"type": "string", "pattern": "^TODO-[0-9]{4,}$"},
    "status": {
      "type": "string",
      "enum": ["proposed", "ready", "in-progress", "blocked", "done", "deferred", "cancelled"]
    },
    "priority": {"type": "string", "enum": ["P0", "P1", "P2", "P3"]}
  },
  "additionalProperties": true
}
```
