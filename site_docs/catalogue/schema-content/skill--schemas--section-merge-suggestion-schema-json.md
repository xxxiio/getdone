---
title: "Read-only Markdown section merge suggestion"
description: "Machine-readable source for skill/schemas/section-merge-suggestion.schema.json."
tags: ["catalogue", "schema"]
---

<!-- Generated by python development/scripts/generate_docs.py. Do not edit directly. -->
# Read-only Markdown section merge suggestion

Authoritative source: `skill/schemas/section-merge-suggestion.schema.json`

## Complete source

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/getdone/section-merge-suggestion.schema.json",
  "title": "Read-only Markdown section merge suggestion",
  "type": "object",
  "required": ["available", "summary", "changes", "notes"],
  "properties": {
    "available": {"type": "boolean"},
    "summary": {
      "type": "object",
      "required": [
        "added",
        "removed",
        "heading-changed",
        "content-changed",
        "reordered",
        "unchanged"
      ],
      "properties": {
        "added": {"type": "integer", "minimum": 0},
        "removed": {"type": "integer", "minimum": 0},
        "heading-changed": {"type": "integer", "minimum": 0},
        "content-changed": {"type": "integer", "minimum": 0},
        "reordered": {"type": "integer", "minimum": 0},
        "unchanged": {"type": "integer", "minimum": 0}
      },
      "additionalProperties": false
    },
    "changes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "kind",
          "installed_heading",
          "available_heading",
          "installed_level",
          "available_level",
          "installed_occurrence",
          "available_occurrence",
          "installed_order",
          "available_order",
          "confidence",
          "reason",
          "diff"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "added",
              "removed",
              "heading-changed",
              "content-changed",
              "reordered"
            ]
          },
          "installed_heading": {"type": ["string", "null"]},
          "available_heading": {"type": ["string", "null"]},
          "installed_level": {"type": ["integer", "null"], "minimum": 1, "maximum": 6},
          "available_level": {"type": ["integer", "null"], "minimum": 1, "maximum": 6},
          "installed_occurrence": {"type": ["integer", "null"], "minimum": 1},
          "available_occurrence": {"type": ["integer", "null"], "minimum": 1},
          "installed_order": {"type": ["integer", "null"], "minimum": 0},
          "available_order": {"type": ["integer", "null"], "minimum": 0},
          "confidence": {"type": ["number", "null"], "minimum": 0, "maximum": 1},
          "reason": {"type": "string"},
          "diff": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "notes": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "additionalProperties": false
}
```
