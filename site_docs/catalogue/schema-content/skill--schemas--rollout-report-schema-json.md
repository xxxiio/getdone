---
title: "v1 rollout validation report"
description: "Machine-readable source for skill/schemas/rollout-report.schema.json."
tags: ["catalogue", "schema"]
---

<!-- Generated by python development/scripts/generate_docs.py. Do not edit directly. -->
# v1 rollout validation report

Authoritative source: `skill/schemas/rollout-report.schema.json`

## Complete source

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/getdone/rollout-report.schema.json",
  "title": "v1 rollout validation report",
  "type": "object",
  "required": ["schema_version", "release_target", "summary", "cases", "limitations"],
  "properties": {
    "schema_version": {"const": 1},
    "release_target": {"type": "string", "minLength": 1},
    "summary": {
      "type": "object",
      "required": ["case_count", "task_classes", "languages", "adapters", "all_passed"],
      "properties": {
        "case_count": {"type": "integer", "minimum": 3},
        "task_classes": {"type": "array", "items": {"type": "string"}, "uniqueItems": true},
        "languages": {"type": "array", "items": {"type": "string"}, "minItems": 2, "uniqueItems": true},
        "adapters": {"type": "array", "items": {"type": "string"}, "minItems": 2, "uniqueItems": true},
        "all_passed": {"const": true}
      },
      "additionalProperties": false
    },
    "cases": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "object",
        "required": [
          "id", "task_class", "language", "adapter", "before", "after",
          "adapter_status", "project_validation", "context_documents", "context_tokens"
        ],
        "properties": {
          "id": {"type": "string", "minLength": 1},
          "task_class": {"type": "string", "enum": ["feature", "bug-fix", "refactoring", "investigation"]},
          "language": {"type": "string", "minLength": 1},
          "adapter": {"type": "string", "minLength": 1},
          "before": {"type": "string", "enum": ["expected-failure", "passed"]},
          "after": {"const": "passed"},
          "adapter_status": {"type": "string", "enum": ["created", "bootstrap-managed", "reference-only"]},
          "project_validation": {"const": "passed"},
          "context_documents": {"type": "integer", "minimum": 1},
          "context_tokens": {"type": "integer", "minimum": 1}
        },
        "additionalProperties": false
      }
    },
    "limitations": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "minItems": 1
    }
  },
  "additionalProperties": false
}
```
