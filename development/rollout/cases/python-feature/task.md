# Task

Implement `render_slug` test-first. It must normalise runs of non-alphanumeric characters,
strip separators from both ends, support a caller-supplied separator, and reject an empty
separator.
