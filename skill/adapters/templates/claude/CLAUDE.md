# Claude Code Instructions

Read `AGENTS.md`; verify `.agent/skills.lock.json`; use `.agent/skills-reference.md`
to locate the shared checkout; read `skill/START-HERE.md`; classify through
`skill/workflow-router.md`; load only selected context; keep mutable state in `.agent/`;
and consult `skill/registry/reuse-catalogue.json` only when a reuse trigger applies.
