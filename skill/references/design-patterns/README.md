# Design Pattern References

Pattern references are loaded only when the core standard identifies real variation,
lifecycle, or boundary complexity. Search with:

```bash
getdone-search --repository-root <skills-root> --kind design-pattern --query <need>
```

Prefer direct code when a pattern adds more concepts or indirection than the change it
supports. Related decision records compare patterns with simpler alternatives.
