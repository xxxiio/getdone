# Shared-Component References

These references describe reusable capability boundaries; they are not a mandatory
internal library. Load one only when a task is about common infrastructure or repeated
semantics.

Use `component-checklist.md` before promoting project code. Search with:

```bash
getdone-search --repository-root <skills-root> --kind shared-component --query <need>
```
