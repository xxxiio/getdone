# Sample Consuming Project

Run the bootstrap tool against a temporary directory to see the generated `.agent/` structure:

```bash
python getdone/initialise_project.py \
  --project-root /tmp/sample-project \
  --project-name "Sample Project" \
  --profile standard
```
