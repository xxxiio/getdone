# GetDone Golden-Path Example

A deliberately small Python project used by the golden-path tutorial. The feature is a
validated greeting function. It demonstrates a bounded change with observable tests rather
than a large framework.

Run:

```bash
python -m unittest discover -s tests
```

To practise the complete lifecycle, copy this directory, bootstrap it with `getdone init`,
and follow `docs/golden-path.md`.
